import { STRINGS } from "../../Constants/Strings"

// export interface commonState{
//     common:{
//         [STRINGS.IS_LOGGED_IN]:boolean;
//     };
// }

export interface commonState {
  common: {
    [STRINGS.IS_LOGGED_IN]: boolean;
  };
}
